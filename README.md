# CLI APP NCC PLUS

- `.env` file structure :
  `env
POSTGRES_USERNAME=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DATABASE=
`

* Using 2 files on [this folder](./postgres/) to initiate tables and insert data.
